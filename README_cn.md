# 零点效应探索器

交互式工具，帮助理解**零点**如何改变二阶系统的阶跃响应——这是控制理论中出了名难以建立直觉的核心概念。

## 包含内容

| 文件 | 说明 | 打开方式 |
|------|------|----------|
| `zero_effect_explorer.html` | 自包含的交互式网页 | 任意浏览器——无需服务器，无需安装 |
| `pid_explorer.html` | PID 控制器探索器——观察 Kp、Kd、Ki 如何改变 ζ_eff、ωₙ_eff 和阶跃响应 | 任意浏览器——无需服务器，无需安装 |
| `zero_effect_demo.py` | 生成 3 张图的 Python 脚本 | `python3 zero_effect_demo.py`（需要 `control`、`matplotlib`） |
| `zero_effect_video_script.md` | 英文视频脚本（7 个场景，约 12-15 分钟） | 任意文本编辑器 |
| `zero_effect_video_script_cn.md` | 哔哩哔哩中文视频脚本（7 个场景） | 任意文本编辑器 |

## 立刻体验

```bash
# 交互式网页——直接打开即可：
open zero_effect_explorer.html   # 零点效应探索器
open pid_explorer.html           # PID 控制器探索器

# 或者运行 Python 演示：
pip install control matplotlib
python3 zero_effect_demo.py
```

## 你将学到什么

**PID 探索器**——控制器增益如何重塑闭环动态：
- Kp 提高有效 ωₙ（响应更快），但降低有效 ζ（超调更大）
- Kd 增加阻尼——将主导极点向左拉，提升 ζ
- Ki 通过在 s=0 处引入积分器极点来消除稳态误差
- PD 让你可以独立调节 ζ_eff 和 ωₙ_eff；PI 消除静差但会拖长调节时间
- 有效 ζ 和 ωₙ 由闭环主导极点对计算得出——底层使用卡尔丹诺三次方程公式

**零点效应探索器**——零点如何改变二阶系统的阶跃响应：

**左半平面（LHP）零点**——速度与超调的权衡：
- 远离原点的零点几乎不可见
- 零点越靠近原点，微分"冲击"越大，超调增加但上升时间缩短
- 这正是 PID 微分作用在做的事——你在放置一个零点，而*放在哪里*至关重要

**右半平面（RHP）零点**——硬性的物理限制：
- 系统初始会朝**错误的方向**运动，之后才恢复
- 越靠近原点 = 反调越深、越持久
- 任何控制器都无法消除 RHP 零点；它施加了大约 |z|/2 的带宽限制
- 现实案例：锅炉汽包水位——加入冷水后水位先下降再上升

**多个零点**——效果叠加：
- 每增加一个零点就增加一个微分项
- 两个 LHP 零点比单个零点的超调更大

## PID 探索器

`pid_explorer.html` 将同样的交互式方法应用于二阶对象的 PID 控制：

- **实时阶跃响应**——基于 PID 状态空间模型的闭环 RK4 仿真，叠加开环对象响应作为对比
- **实时零极点图**——闭环极点、控制器零点，以及半透明的开环对象极点；稳定性边界高亮显示
- **滑块**——对象参数 ζ、ωₙ，加上颜色编码的 PID 增益（Kp 粉色、Kd 青色、Ki 绿色）
- **实时读数**——有效 ζ_eff、ωₙ_eff、超调百分比和稳态误差
- **7 个预设**——覆盖纯 P、PD、PI、PID 整定、激进参数、大 D 和无控制基准
- **动态解读**——底部面板根据当前工作点解释每个增益的作用
- **不稳定检测**——右半平面极点触发红色覆盖层和警告标识

### 🔑 一个核心洞见：积分器极点会移动

当你滑动 Ki 滑块时，会立刻看到以下现象——这**不是**计算错误：

PID 控制器的积分项（Ki/s）在 **s = 0** 处引入一个极点。在**开环**中，这个极点就停在原点。但当反馈回路闭合后，它**移动了**。以"PID Tuned"预设为例（ζ=0.5, ωₙ=2, Kp=3, Kd=0.8）：

| Ki | 实极点（原本在 s=0） | 共轭复极点对 | 现象 |
|----|----------------------|-------------|------|
| 0（PD） | 抵消 | −2.6 ± j3.0 | 无积分器——稳态误差残留 |
| 0.4 | **−0.103** | −2.55 ± j3.0 | 实极点靠近原点 → 拖出长长的调节尾巴 |
| 2 | −0.48 | −2.36 ± j3.2 | 积分器加速，共轭复极点对向右漂移 |
| 10 | −3.77 | −0.71 ± j3.2 | 共轭复极点对逼近虚轴——Ki 太大了！ |

**这是 PID 控制的基本特性**：积分作用将稳态误差拉向零，但随着 Ki 增大，实极点向左移动（积分器加快），而共轭复极点对向虚轴靠近。Ki 调得太大，复极点将越过虚轴进入右半平面——系统失稳。

从根轨迹的角度看：积分器极点从 s=0 出发沿负实轴移动，而两个对象极点先彼此靠拢，然后随 Ki 增大分离。PID 探索器实时展示这一切——滑动 Ki 时请紧盯零极点图。

## 交互式零点效应探索器

`zero_effect_explorer.html` 是一个约 1180 行的单文件 HTML，零依赖：

- **实时阶跃响应**——通过状态空间（能控标准型）+ RK4 仿真计算
- **实时零极点图**——展示参数调整时极点和零点的移动
- **滑块**——阻尼比 ζ、自然频率 ωₙ，以及最多 3 个零点（每个可跨越 LHP ↔ RHP）
- **7 个预设**——覆盖关键场景：远/近 LHP 零点、远/近 RHP 零点、双零点、混合
- **动态解读**——底部面板根据当前配置解释你看到的现象
- **合成曲线**——当激活 2 个以上零点时，白色虚线显示它们的复合效果

## 截图

运行 `python3 zero_effect_demo.py` 生成以下三张图：

- `zero_effect_one_zero.png` — LHP 零点（左）和 RHP 零点（右）与基准对比
- `zero_effect_two_zeros.png` — 一个 vs. 两个 LHP 零点，带快速第三极点
- `zero_effect_pzmap.png` — 示例配置的零极点图

## 环境要求

- **网页探索器**：任意现代浏览器（Chrome、Firefox、Safari、Edge）
- **Python 演示**：Python 3.9+、`control >= 0.10`、`matplotlib >= 3.6`、`numpy`

## 许可证

MIT
